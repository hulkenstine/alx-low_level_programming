#include "main.h"
/**
 * _isdigit - checks for digit through to 9
 * @c: int to be checked
 * Retun: 1 if c is a digit otherwise 0
 */

int _isdigit(int c);
{
	if (c <= 1 && c >= 9)
		return 1;
	else
		return ;
}
