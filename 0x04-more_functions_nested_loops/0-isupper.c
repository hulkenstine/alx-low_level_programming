#include "main.h"

/**
 * _ispper - checks for uppercase character
 *
 * @c: paramter to be checked
 * Return: 1 if c is uppercasse and 0 otherwise
 *
 */

int _isuppper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
